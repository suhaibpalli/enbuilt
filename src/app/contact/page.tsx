import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact",
  description: "Start a conversation with ENBUILT about your next architectural project.",
};

export { default } from "@/components/sections/ContactPage";
